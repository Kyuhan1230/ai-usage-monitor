생성일시: 2026-07-19 17:27:49 +09:00
수정일시: 2026-07-19 17:31:00 +09:00

## 이번 단계에서 한 일

- Windows 알림의 조건, 중복 억제 서명과 본문 생성을 순수 함수로 분리했습니다.
- 한도 임계치와 리셋 전 고갈 예상 이유를 알림 본문에 표시하도록 개선했습니다.
- Insights에만 표시되던 토큰 이상 급증을 Windows 알림에도 포함해 README의 기능 설명과 실제 동작을 일치시켰습니다.
- 한도 알림과 이상 급증 알림이 없으면 알림을 만들지 않는 테스트를 추가했습니다.

## 잘한 점

- 단순히 남은 퍼센트만 보여주지 않고 사용자가 알림 이유를 바로 알 수 있습니다.
- 이상 급증만 발생한 경우에도 능동적으로 알림을 받을 수 있습니다.
- 한도와 이상 급증을 함께 서명하므로 내용이 바뀌지 않은 동일 알림은 실행 중 반복하지 않습니다.

## 부족한 점 / 남은 리스크

- 중복 억제 서명은 프로세스 메모리에만 있어 앱을 다시 시작하면 같은 위험을 한 번 더 알릴 수 있습니다.
- Windows 알림 자체가 사용자 OS 설정에서 차단된 경우 앱이 이를 강제로 표시할 수 없습니다.

## 다음 단계

- 휴대용 C++ 컴파일러와 격리 Windows SDK를 사용해 Rust 전체 테스트와 실제 Tauri 실행을 시도합니다.

## 테스트 결과

- `npm run test:ui`: 통과
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`: 통과
- `git diff --check`: 통과
- Rust 단위 테스트: 휴대용 C++ 도구 준비 후 실행 예정

## 출력 불변성 검증 결과

- 기존 Windows 알림 제목과 Tauri notification plugin을 유지했습니다.
- 분석 결과의 `alerts`·`anomalies` 구조, 임계치와 수집 경로는 변경하지 않았습니다.
