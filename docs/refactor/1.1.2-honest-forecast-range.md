생성일시: 2026-07-19 16:54:45 +09:00
수정일시: 2026-07-20 +09:00

## 이번 단계에서 한 일

- 5분 단위 정수 잔여율의 순간 변화 대신 짧은 한도는 30분, 주간 한도는 2시간 단위로 묶어 소진 속도를 계산합니다.
- 기존 단일 `expectedExhaustionAt` 필드는 호환을 위해 유지했습니다.
- 신뢰도를 원시 표본 수가 아니라 관찰 시간, 실제 잔여량 감소 횟수, 집계 구간 수와 평균 속도 오차로 판정합니다.
- Insights는 `기록 부족`을 반복하지 않고 실제로 부족한 근거와 다음 행동을 설명합니다.
- Compact는 각 한도의 최근 시간당 소진 속도를 잔여율 옆에 표시합니다.

## 잘한 점

- 기존 분석 파일 소비자를 깨뜨리지 않는 확장 방식입니다.
- 분 단위 시각을 정답처럼 보이게 하던 과도한 정밀도를 줄였습니다.
- 속도가 일정해 보여도 최소 10% 폭을 적용해 관측 패턴 변화 가능성을 남겼습니다.
- 실제 Codex 주간 기록에서 기존 약 70시간이던 범위가 약 8시간으로 줄어, 리셋 전 소진 판정을 더 빠르게 읽을 수 있습니다.

## 부족한 점 / 남은 리스크

- 예측 범위는 통계적 신뢰구간이 아니라 집계 구간 속도의 MAD와 관찰 구간 수에 기반한 실용적 오차 범위입니다.
- 활동 시간과 유휴 시간을 아직 별도로 모델링하지 않습니다.
- 과거 예측과 실제 소진 결과를 비교하는 사후 오차 추적은 아직 없습니다.

## 다음 단계

- 자동 감시 정책 확정 전까지 현재 수동 수집 원칙을 유지하고, 전체 테스트 및 UI 렌더 검증을 보강합니다.

## 테스트 결과

- `npm run test:ui`: 통과
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`: 통과
- `git diff --check`: 통과
- Rust 단위 테스트: MSVC `link.exe` 부재로 실행 불가

## 출력 불변성 검증 결과

- 기존 `expectedExhaustionAt`, `depletionRatePercentPerHour`, `willExhaustBeforeReset`, `confidence` 필드를 유지했습니다.
- `depletionEventCount`, `forecastSpreadPercent`를 추가해 UI가 원시 표본 수와 실제 판단 근거를 구분할 수 있습니다.
- 비용, 비교, 이상 감지와 추천 계산에는 변경을 가하지 않았습니다.
