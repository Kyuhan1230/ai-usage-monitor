생성일시: 2026-07-19 16:52:14 +09:00
수정일시: 2026-07-19 16:54:00 +09:00

## 이번 단계에서 한 일

- Codex 또는 Claude 중 하나만 인증해도 첫 설정을 완료할 수 있도록 변경했습니다.
- 수동 새로고침은 설치된 CLI만 실행하고, 설치하지 않은 공급자를 수집 오류로 취급하지 않도록 변경했습니다.
- 두 도구가 모두 없을 때만 연결 안내 오류를 반환합니다.

## 잘한 점

- 한 공급자만 쓰는 사용자가 `나중에` 버튼으로 우회하지 않아도 됩니다.
- 기존 두 공급자 병렬 수집은 둘 다 설치된 경우 그대로 유지됩니다.
- 공급자별 상태 파일과 분석 JSON 형식을 변경하지 않았습니다.

## 부족한 점 / 남은 리스크

- 설치되어 있지만 로그인하지 않은 CLI는 수집을 시도한 뒤 해당 공급자 오류로 표시됩니다.
- Rust 툴체인은 확인했지만 MSVC `link.exe`가 없어 `cargo test`와 `cargo clippy`의 컴파일 단계는 실행하지 못했습니다.

## 다음 단계

- 고갈 시각의 과도한 정밀도를 낮추고 예측 범위와 변동성 기반 신뢰 근거를 추가합니다.

## 테스트 결과

- `npm run test:ui`: 통과
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`: 통과
- `git diff --check`: 통과
- `npm test`: MSVC `link.exe` 부재로 Rust 의존성 컴파일 단계에서 중단

## 출력 불변성 검증 결과

- 로컬 저장 경로, 상태 파일 이름, 분석 결과 필드와 수집 명령은 유지했습니다.
- 설치된 두 CLI가 있을 때의 병렬 원샷 수집 동작은 유지했습니다.
